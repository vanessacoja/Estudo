/** @type {import('next').NextConfig} */
const nextConfig = {
  imagens:{
    remoteParatterns: [
       {
         protocol: 'https',
         hostname:'dogsapi.origamid.dev'
       }

    ]
  }







};

export default nextConfig;
 